BOT_TOKEN=123456:ABC-from-BotFather
# Railway: добавь PostgreSQL и вставь переменную DATABASE_URL из панели
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Необязательно
DEFAULT_TZ=Europe/Moscow
REMINDER_POLL_SECONDS=5
MIN_SPAM_INTERVAL_SECONDS=15
READ_ACK_INTERVAL_SECONDS=30

# Mini App: публичный HTTPS URL корня (где открывается /), тот же хост что у uvicorn-сервиса
# WEBAPP_PUBLIC_URL=https://your-web.railway.app
# Запуск API: uvicorn bot.miniapp_api:app --host 0.0.0.0 --port $PORT
